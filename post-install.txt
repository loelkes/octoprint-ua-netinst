#!/bin/sh

# System setting
cp /bootfs/setup-octoprint.sh /rootfs/root/
echo "@reboot root /root/setup-octoprint.sh firstboot >> /var/log/octoprint-ua-netinst.log 2>&1" > /rootfs/etc/cron.d/octoprint-install
