#!/bin/sh

# System setting
cp /bootfs/octoprint-ua-netinst.sh /rootfs/root/
echo "@reboot root /root/octoprint-ua-netinst.sh firstboot >> /var/log/octoprint-ua-netinst.log 2>&1" > /rootfs/etc/cron.d/octoprint-install
